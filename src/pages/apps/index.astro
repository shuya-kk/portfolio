---
import appsData from "../../data/apps.json";
import Layout from "../../layouts/Layout.astro";
import AppCard from "../../components/AppCard.astro";
import SectionHeader from "../../components/SectionHeader.astro";

const apps = appsData || [];
---

<Layout title="制作物">
    <div class="max-w-6xl mx-auto px-8 py-32">
        <div class="mb-32">
            <SectionHeader title="制作物" subtitle="iOSアプリとこだわり" />
            <p
                class="text-center text-zen-dim serif-sub max-w-xl mx-auto mt-[-2rem] leading-relaxed"
            >
                機能性、シンプルさ、そして洗練された体験を追求した<br
                    class="hidden md:block"
                />iOSアプリケーションのコレクションです。
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 lg:gap-16">
            {
                apps.map((app, index) => (
                    <AppCard
                        name={app.name}
                        description={app.description}
                        iconUrl={app.iconPlaceholder}
                        appStoreUrl={app.appStoreLink}
                        features={app.features}
                        isFeatured={index === 0}
                    />
                ))
            }
        </div>
    </div>
</Layout>
