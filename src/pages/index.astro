---
import { getCollection } from "astro:content";
import Layout from "../layouts/Layout.astro";
import AppCard from "../components/AppCard.astro";
import BlogCard from "../components/BlogCard.astro";
import SectionHeader from "../components/SectionHeader.astro";
import appsData from "../data/apps.json";

const allPosts = await getCollection("blog");
const latestPosts = allPosts
    .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
    .slice(0, 4);

const featuredApps = appsData.slice(0, 3);
---

<Layout title="PORTFOLIO">
    <!-- Premium Hero Section -->
    <section
        id="hero"
        class="min-h-screen flex items-center justify-center relative px-8 py-32 overflow-hidden bg-zen-bg"
    >
        <!-- Sophisticated Accents -->
        <div class="absolute inset-0 opacity-[0.05] pointer-events-none">
            <div class="absolute top-1/4 right-1/4 w-px h-64 bg-zen-gold/30">
            </div>
            <div
                class="absolute bottom-1/4 left-1/4 w-32 h-px bg-zen-indigo/20"
            >
            </div>
        </div>

        <div
            class="relative z-10 w-full max-w-6xl mx-auto flex flex-col md:flex-row items-center justify-between gap-24"
        >
            <!-- Left Side: Refined Vertical Typography -->
            <div
                class="hidden md:flex flex-col items-center gap-12 vertical-rl animate-sumi"
            >
                <span
                    class="text-[10px] font-bold tracking-[1em] text-zen-gold uppercase"
                    >iOS Developer</span
                >
                <div class="w-px h-32 bg-zen-gold opacity-20"></div>
                <h2
                    class="text-6xl lg:text-8xl font-black serif-heading text-zen-ink tracking-widest leading-none"
                >
                    作品集
                </h2>
            </div>

            <!-- Main Content -->
            <div class="flex-grow max-w-2xl text-center md:text-left">
                <div class="mb-12 sig-mark animate-sumi">Digital Artisan</div>

                <h1
                    class="text-5xl md:text-7xl font-light mb-10 serif-heading text-zen-ink leading-tight"
                >
                    感覚に響く、<br />
                    シンプルな道具を。
                </h1>

                <p
                    class="text-lg md:text-xl text-zen-dim serif-sub mb-20 leading-loose max-w-lg"
                >
                    使い心地の良さを追求し、<br />
                    日常に溶け込む形を仕立てる。
                </p>

                <!-- Actions -->
                <div
                    class="flex flex-wrap items-center justify-center md:justify-start gap-16"
                >
                    <a
                        href="/portfolio/apps"
                        class="group relative text-xs font-bold tracking-[0.5em] text-zen-ink flex items-center gap-6"
                    >
                        <span
                            class="w-12 h-px bg-zen-gold group-hover:w-20 transition-all duration-700"
                        ></span>
                        制作物をみる
                    </a>
                    <a
                        href="/portfolio/#about"
                        class="group relative text-xs font-bold tracking-[0.5em] text-zen-ink flex items-center gap-6"
                    >
                        <span
                            class="w-12 h-px bg-zen-gold group-hover:w-20 transition-all duration-700"
                        ></span>
                        志について
                    </a>
                </div>
            </div>

            <!-- Right Visual Accent -->
            <div
                class="hidden lg:block w-px h-96 bg-gradient-to-b from-transparent via-zen-gold/40 to-transparent opacity-30"
            >
            </div>
        </div>

        <!-- Scroll Indicator -->
        <div
            class="absolute bottom-12 left-1/2 -translate-x-1/2 flex flex-col items-center gap-4 opacity-30"
        >
            <span
                class="vertical-rl text-[9px] font-bold tracking-[0.4em] uppercase"
                >Scroll</span
            >
            <div class="w-px h-12 bg-zen-ink animate-pulse"></div>
        </div>
    </section>

    <!-- Apps Section -->
    <section
        id="apps"
        class="max-w-6xl mx-auto px-8 py-32 border-t border-zen-border/30"
    >
        <SectionHeader title="作品集" subtitle="iOSアプリとこだわり" />
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 lg:gap-16">
            {
                featuredApps.map((app, index) => (
                    <AppCard
                        name={app.name}
                        description={app.description}
                        iconUrl={app.iconPlaceholder}
                        appStoreUrl={app.appStoreLink}
                        features={app.features}
                        isFeatured={index === 0}
                    />
                ))
            }
        </div>

        <div class="mt-28 text-center">
            <a
                href="/portfolio/apps"
                class="group inline-flex items-center gap-4 text-[10px] font-bold tracking-[0.5em] text-zen-dim hover:text-zen-ink transition-colors uppercase"
            >
                すべての作品をみる
                <span
                    class="w-12 h-px bg-zen-border group-hover:w-16 group-hover:bg-zen-ink transition-all"
                ></span>
            </a>
        </div>
    </section>

    <!-- Posts Section -->
    <section id="posts" class="bg-white/40 py-32 border-y border-zen-border/40">
        <div class="max-w-5xl mx-auto px-8">
            <SectionHeader title="記録" subtitle="開発と思索" />
            <div class="space-y-6">
                {
                    latestPosts.map((post) => (
                        <BlogCard
                            title={post.data.title}
                            date={post.data.pubDate}
                            slug={post.slug}
                            tags={post.data.tags}
                        />
                    ))
                }
            </div>

            <div class="mt-24 text-center">
                <a
                    href="/portfolio/blog"
                    class="group inline-flex items-center gap-4 text-[10px] font-bold tracking-[0.5em] text-zen-dim hover:text-zen-ink transition-colors uppercase"
                >
                    記録一覧へ
                    <span
                        class="w-12 h-px bg-zen-border group-hover:w-16 group-hover:bg-zen-ink transition-all"
                    ></span>
                </a>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section id="about" class="max-w-4xl mx-auto px-8 py-32">
        <SectionHeader title="私について" subtitle="理念と歩み" />

        <div
            class="zen-panel p-12 md:p-32 relative overflow-hidden bg-white/60 backdrop-blur-xl flex flex-col md:flex-row gap-24 group"
        >
            <div class="max-w-xl relative z-10 flex-grow">
                <div class="mb-16 sig-mark">PHILOSOPHY</div>

                <p
                    class="text-3xl md:text-5xl text-zen-ink serif-heading mb-20 leading-tight italic"
                >
                    使い心地という、<br />
                    目に見えない美しさを。
                </p>
                <div
                    class="space-y-16 text-zen-dim leading-loose text-lg md:text-xl font-light serif-sub opacity-80"
                >
                    <p>
                        単に「動くもの」を作るのではなく、<br />
                        日々の営みに静かに寄り添う「道具」としてのソフトウェアを仕立てています。
                    </p>
                    <p>
                        伝統的な工芸品が持つ、時を経ても色褪せない機能美と、<br
                        />
                        現代のテクノロジーがもたらす滑らかな体験の融合。
                    </p>
                </div>

                <div
                    class="mt-24 pt-20 border-t border-zen-border flex flex-wrap items-center justify-between"
                >
                    <div class="flex gap-12">
                        <a
                            href="https://github.com"
                            target="_blank"
                            class="text-[11px] font-bold tracking-[0.6em] text-zen-indigo/60 hover:text-zen-gold transition-colors uppercase"
                            >GitHub</a
                        >
                    </div>

                    <div
                        class="text-[10px] font-black tracking-[0.2em] text-zen-ink sig-mark"
                    >
                        SHUYA
                    </div>
                </div>
            </div>

            <!-- Artisan Border Accent -->
            <div
                class="hidden lg:flex flex-col items-center justify-center gap-12 opacity-20 group-hover:opacity-40 transition-opacity duration-1000"
            >
                <div class="w-px h-32 bg-zen-gold"></div>
                <span
                    class="vertical-rl text-[10px] font-bold tracking-[1em] uppercase"
                    >Est. 2025</span
                >
                <div class="w-px h-32 bg-zen-gold"></div>
            </div>
        </div>
    </section>
</Layout>
```
