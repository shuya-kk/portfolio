---
import appData from "../data/apps.json";
import { getCollection } from "astro:content";
import CanvasBackground from "../components/CanvasBackground.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import AppCard from "../components/AppCard.astro";
import BlogCard from "../components/BlogCard.astro";
import SectionHeader from "../components/SectionHeader.astro";

const allPosts = await getCollection("blog");
const sortedPosts = allPosts.sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const latestPosts = sortedPosts.slice(0, 4);

const recentApps = appData.slice(0, 3);
---

<!doctype html>
<html lang="ja" class="scroll-smooth">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Shuya Komiya | Portfolio</title>
        <link rel="icon" href="/favicon.ico" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;900&family=JetBrains+Mono:wght@400&display=swap"
            rel="stylesheet"
        />
    </head>
    <body
        class="bg-dark-bg text-text-main font-sans selection:bg-neon-amber selection:text-white overflow-x-hidden"
    >
        <CanvasBackground />
        <Navbar />

        <main class="relative z-10">
            <!-- Hero Section -->
            <section
                id="hero"
                class="min-h-screen flex items-center justify-center relative px-4"
            >
                <div class="max-w-5xl mx-auto text-center">
                    <div
                        class="inline-block mb-6 animate-fade-in opacity-0"
                        style="animation-delay: 0.2s;"
                    >
                        <span
                            class="px-4 py-2 rounded-full border border-neon-amber/30 bg-neon-amber/5 text-neon-amber text-sm font-mono tracking-wider"
                        >
                            IOS DEVELOPER & UI DESIGNER
                        </span>
                    </div>

                    <h1
                        class="text-6xl sm:text-8xl md:text-9xl font-black tracking-tighter leading-none mb-8 animate-fade-in-up opacity-0"
                        style="animation-delay: 0.4s;"
                    >
                        DIGITAL<br />
                        <span
                            class="text-transparent bg-clip-text bg-gradient-to-r from-neon-amber to-neon-gold text-glow"
                            >CRAFTSMAN</span
                        >
                    </h1>

                    <p
                        class="text-xl text-text-muted max-w-2xl mx-auto mb-12 animate-fade-in-up opacity-0"
                        style="animation-delay: 0.6s;"
                    >
                        Building immersive digital experiences with a focus on
                        motion, aesthetics, and usability.
                    </p>

                    <div
                        class="animate-fade-in-up opacity-0"
                        style="animation-delay: 0.8s;"
                    >
                        <a
                            href="#apps"
                            class="group inline-flex items-center gap-3 px-8 py-4 bg-white text-black rounded-full font-bold hover:bg-neon-amber hover:text-white transition-all duration-300 hover:scale-105"
                        >
                            Explore Work
                            <svg
                                class="w-4 h-4 group-hover:translate-y-1 transition-transform"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                                ><path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M19 14l-7 7m0 0l-7-7m7 7V3"></path></svg
                            >
                        </a>
                    </div>
                </div>

                <!-- Scroll Indicator -->
                <div
                    class="absolute bottom-10 left-1/2 transform -translate-x-1/2 animate-float opacity-50"
                >
                    <div
                        class="w-6 h-10 border-2 border-white/20 rounded-full flex justify-center p-1"
                    >
                        <div
                            class="w-1 h-2 bg-white rounded-full animate-pulse-glow"
                        >
                        </div>
                    </div>
                </div>
            </section>

            <!-- Apps Section -->
            <section id="apps" class="py-32 relative">
                <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                    <SectionHeader
                        title="Selected Works"
                        subtitle="Portfolio"
                    />

                    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                        {
                            recentApps.map((app, index) => (
                                <AppCard
                                    name={app.name}
                                    description={app.description}
                                    iconUrl={app.iconPlaceholder}
                                    appStoreUrl={app.appStoreLink}
                                    features={app.features}
                                    isFeatured={index === 0}
                                />
                            ))
                        }
                    </div>

                    <div class="mt-12 text-center">
                        <a
                            href="/portfolio/apps"
                            class="inline-flex items-center text-text-muted hover:text-neon-amber transition-colors font-mono text-sm group"
                        >
                            VIEW ALL APPS
                            <svg
                                class="w-4 h-4 ml-2 group-hover:translate-x-1 transition-transform"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                                ><path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg
                            >
                        </a>
                    </div>
                </div>
            </section>

            <!-- Blog Section -->
            <section
                id="blog"
                class="py-32 bg-dark-surface/30 border-y border-white/5"
            >
                <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
                    <SectionHeader
                        title="Latest Thoughts"
                        subtitle="Blog"
                        align="center"
                    />

                    <div class="mt-16 space-y-8">
                        {
                            latestPosts.map((post) => (
                                <BlogCard
                                    title={post.data.title}
                                    date={post.data.pubDate}
                                    slug={post.slug}
                                    tags={post.data.tags}
                                />
                            ))
                        }
                    </div>

                    <div class="mt-16 text-center">
                        <a
                            href="/portfolio/blog"
                            class="inline-flex items-center px-6 py-3 border border-white/10 rounded-full text-sm font-bold hover:bg-white hover:text-black transition-all duration-300"
                        >
                            Read More Articles
                        </a>
                    </div>
                </div>
            </section>

            <!-- About Section -->
            <section id="about" class="py-32 relative overflow-hidden">
                <div
                    class="absolute right-0 top-1/2 -translate-y-1/2 w-1/2 h-full bg-gradient-to-l from-neon-amber/5 to-transparent pointer-events-none"
                >
                </div>

                <div
                    class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center"
                >
                    <SectionHeader
                        title="About Me"
                        subtitle="Profile"
                        align="center"
                    />

                    <div
                        class="prose prose-invert prose-lg mx-auto text-text-muted leading-relaxed"
                    >
                        <p>
                            個人開発者として、iOSアプリケーションの開発・運営を行っています。
                            <span class="text-white font-bold">「機能美」</span
                            >と<span class="text-white font-bold"
                                >「心地よさ」</span
                            >が調和するデジタル体験を追求しています。
                        </p>
                        <p>
                            Swift/SwiftUIを中心に、企画から設計、実装、リリースまで一貫して担当。
                            ただ動くものを作るのではなく、ユーザーの感情を動かすようなインターフェース作りを心がけています。
                        </p>
                    </div>

                    <div class="mt-12">
                        <a
                            href="mailto:contact@example.com"
                            class="inline-block px-8 py-4 bg-neon-amber text-white font-bold rounded-full shadow-[0_0_20px_rgba(245,158,11,0.4)] hover:shadow-[0_0_40px_rgba(245,158,11,0.6)] hover:-translate-y-1 transition-all duration-300"
                        >
                            Get In Touch
                        </a>
                    </div>
                </div>
            </section>
        </main>

        <Footer />
    </body>
</html>
